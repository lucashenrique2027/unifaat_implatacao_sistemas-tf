# .env.example - TF09 Portfolio
# Copie para .env e preencha os valores

# Backend
PORT=3000
DB_PATH=/app/data/portfolio.db

# AWS (apenas para referência local, nunca commitar valores reais)
AWS_REGION=us-east-1
EC2_PUBLIC_IP=<seu-ip-publico-ec2>
